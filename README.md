# playshare
a music sharing web application via Bluetooth 
using react.js as frontend, and node.js as backend

//instruction
this project is created using npm toolchain
--install node.js
--to start development
    run npm start on root directory
--to build 
    run npm build

